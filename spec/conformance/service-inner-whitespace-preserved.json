{
  "name": "service-inner-whitespace-preserved",
  "description": "NEGATIVE CONTROL for service-is-trimmed. Trimming must normalize the EDGES only. Without this case, an implementation that collapsed or stripped INTERNAL whitespace would silently rewrite service names and the trim test would still pass — green either way.",
  "configure": {
    "service": "  my service  "
  },
  "call": {
    "fn": "logEvent",
    "event": "demo.event",
    "fields": {}
  },
  "expect": {
    "emitted": true,
    "fields": {
      "service": "my service"
    }
  }
}
